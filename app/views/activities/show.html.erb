<div class="container">
  <div class="row">
    <h1 class="page-header text-center">Show Activity</h1>
    <p><%= @activity.name %></p>
    <%= link_to "add students", :class => "btn btn-success glyphicon glyphicon-plus-sign" %>
      <%= link_to "Delete", activity_path(@activity.id), :class => "btn btn-danger", method: :delete %>
      <%= link_to "Edit", edit_activity_path(@activity.id), :class => "btn btn-warning" %>
  </div>
  <br>
   <%= select("student", "student_id", Student.all.collect {|p| [ p.name ] }, { include_blank: true }) %>
  <!-- link_to "Add Students", students_path, :class => "btn btn-default" %> -->
</div>
